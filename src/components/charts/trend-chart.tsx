export function TrendChart({ title, values }: { title: string; values: number[] }) { const max = Math.max(...values, 1); return <section className="rounded-xl border bg-surface p-5"><h2 className="font-semibold">{title}</h2><div className="mt-6 flex h-36 items-end gap-2" aria-label={`${title} chart`}>{values.map((value, index) => <div className="flex-1 rounded-t bg-brand-soft" key={`${value}-${index}`}><div className="rounded-t bg-brand" style={{ height: `${(value / max) * 100}%` }} /></div>)}</div><p className="text-text-muted mt-3 text-xs">Static illustrative data. Live values will be provided by Frenzone.</p></section>; }
